# .vscode/preview.yml
autoOpen: true # 打开工作空间时是否自动开启所有应用的预览
apps:
  - port:  # 应用的端口
    run: CUDA_VISIBLE_DEVICES=0 python main.py  # 应用的启动命令
    root: ./examples/mnist # 应用的启动目录
    name: mnist # 应用名称
    autoOpen: true # 打开工作空间时是否自动开启预览（优先级高于根级 autoOpen）
    